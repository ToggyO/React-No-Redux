import modal from './reducer';

import * as modalTypes from './types';
import * as modalActions from './actions';
import * as modalSelectors from './selectors';

export { modalTypes, modalActions, modalSelectors };
export default modal;
